# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Tede Mehrtens
email: kontakt@tedemehrtens.de
author: Jan Tede Mehrtens
description: >- # this means to ignore newlines until "baseurl:"
  Informatiker aus dem Norden. Senior Systems Engineer bei Veeam. Technik, Netze und gelegentlich ein Blogpost.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://www.tedemehrtens.de"
lang: en
locale: en_US
timezone: Europe/Berlin
twitter_username: jantede
github_username: jantede
github_repo: tedemehrtens.de

paginate:		5
paginate_path:	"/posts/page:num/"

# Build settings
plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-webp

future: false

defaults:
  - scope:
      path: ""
    values:
      image: /assets/img/default.png
      lang: en
  - scope:
      path: ""
      type: posts
    values:
      layout: post

webp:
  enabled:    true
  quality:    82          # matches our manual cwebp conversion
  img_dir:    ["/assets/img"]
  nested:     true        # also convert images in subdirectories
  formats:    [".jpeg", ".jpg", ".png"]
  regenerate: false       # skip if .webp already exists (faster rebuilds)

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# _headers starts with _ so Jekyll excludes it by default — explicitly include it
# so Cloudflare Workers Static Assets can find it in _site/
include:
  - _headers

exclude:
  - _archive/
  - .sass-cache/
  - .jekyll-cache/
  - Gemfile
  - Gemfile.lock
  - vendor/
  - wrangler.toml
  - CLAUDE.md
